{
	"name": "@app/api",
	"version": "1.0.8",
	"private": true,
	"scripts": {
		"build": "mkdir -p dist && touch dist/index.js && echo 'Simulating build...'",
		"ci:build": "mkdir -p dist && touch dist/index.js && echo 'Simulating build...'",
		"ci:lint": "echo 'Simulating lint...'",
		"ci:test:unit": "echo 'Simulating unit tests...'",
		"ci:test:unit:shard": "echo 'Simulating unit tests (shard)...'",
		"lint": "echo 'Simulating lint...'",
		"gen:prisma-all": "echo 'Simulating Prisma client generation...'",
		"test:unit": "echo 'Simulating unit tests...'"
	}
}
